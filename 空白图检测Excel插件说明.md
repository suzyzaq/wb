# 空白图检测 · Excel 批量插件（一键启动版 · v4.1）

把「纯白图 / 白底图」识别封装成一个**双击即用**的桌面插件，专门解决：
> 给一份 Excel（商品图 / 主图），自动找出里面哪些是空白图 / 纯色空图，并把结果写回 Excel。

**v4.1 重点升级（长任务防丢 + 中途取用）**：
- 💾 **运行中定期落盘**：每完成 300 个 URL（或每 60 秒）把进度写一次盘。
  长任务若中途断电 / 崩溃 / 被关闭，**已完成的判定结果不丢**，重启后可用 `--resume` 接着跑
- ⏏️ **运行中导出「已完成部分」**：不必等全部跑完 —— 一键导出一份含已完成行的结果 Excel
  （未完成的行标注「未处理」），随时可用、可交付
  - GUI：处理中卡片里的「**导出已完成部分**」按钮
  - CLI：`WhiteImageExcel.exe 表.xlsx --export-partial 表_空白图结果.xlsx.progress.json -o 部分.xlsx`

**v4 已具备（沿用）**：
- 📦 **超过 1000 行自动分批**：默认每 1000 行一批（可调），逐批执行、逐批产出独立 Excel
- ⬇️ **分批可下载**：每完成一批，界面立即出现「下载本批」按钮，**不用等全量跑完**就能先取走结果
- 📥 **最终全量下载**：全部批次跑完后自动合并成一个全量 Excel，一键下载
  - 全量表 = 各批「检测结果」行拼接 + 重新统计的全量「汇总」表
  - 分批文件与全量文件同时保留，可对照核查

**v3 已具备（沿用）**：
- ⚖️ **双维度同时返回**：一次扫描，两种判定维度的结果同时输出到 Excel 不同列
  - **B · 一张即不合规**：任意一张图片为空白，整行判为不合规（最严格 → 多抓）
  - **A · 全部空白才不合规**：仅当该行所有图片均为空白，才判为不合规（最宽松 → 少漏）
  - UI 上两个 checkbox 都默认勾上，可任选/多选；不勾则不输出对应列

**v2 已具备（沿用）**：
- 🖥️ **HTML 向导界面**：双击 exe → 自动打开浏览器 → 卡片式三步向导（上传 / 字段+维度 / 参数）
- 🎛️ **Excel 自定义字段**：上传后从表头勾选「图片地址列」（可多选，即自定义检测字段）

## 三个核心承诺

| 要求 | 实现方式 |
|------|----------|
| **一键启动** | 双击 `WhiteImageExcel.exe` → 浏览器自动打开向导 → 三步点完出结果 |
| **内含安装环境** | PyInstaller 单文件打包，已内置 Python 3.13 + numpy/Pillow/openpyxl/requests，**用户机器无需装任何环境** |
| **不消耗 token** | 纯像素分析（NumPy+Pillow），**不调 AI 模型 / 不调 Dify / 不联网推理**，完全离线判定，0 token 费用 |
| **返回空白图至 Excel** | 结果写回新 Excel，含「检测结果」+「汇总」两表，每行给出两种维度的判定 |

## 使用步骤（图形界面 / HTML 向导）

1. 把待检测的 Excel（如 `商品.xlsx`）放到任意位置。
2. 双击 `dist/WhiteImageExcel.exe` → 自动打开浏览器（若未自动打开，按提示访问 `http://localhost:7862`）。
3. **Step 1 选 Excel**：拖入或点击选择 `.xlsx`，自动解析表头。
4. **Step 2 字段与维度**：
   - 勾选「图片地址列」（可多选 = 自定义字段；含「图/主图/url」的列会自动预勾选）。
   - 勾选「不合规判定维度」（**默认两个都勾上**，输出双列；可任选/全不选）：
     - ☑ **B · 一张即不合规**
     - ☑ **A · 全部空白才不合规**
5. **Step 3 参数**：
   - 并发线程数（默认 8）、近白像素阈值（默认 0.99）
   - **分批行数上限**（默认 **1000**）—— 超过该行数即自动分批
6. 点「开始检测」：
   - 顶部显示「分批执行：已完成 X/Y 批」
   - **每批完成的瞬间**，下方「已完成的批次」卡片出现该批记录 + **「下载本批」按钮**（可立即取走）
   - 全部完成后，结果区显示双维度统计 + **「下载全量结果 Excel」** + 各批次的单独下载入口

> 结果文件名：分批时 `{源文件名}_空白图结果_批次N.xlsx`，全量 `{源文件名}_空白图结果_全量.xlsx`。
> 未超过分批上限时，直接输出 `{源文件名}_空白图结果.xlsx`（不分批）。

## 分批机制说明

| 场景 | 行为 |
|------|------|
| 数据行数 ≤ 分批上限（默认 1000） | 单批执行，直接产出全量文件，不产生批次文件 |
| 数据行数 > 分批上限 | 按上限切分（如 2500 行 / 1000 → 3 批：1000 + 1000 + 500），逐批产出并支持分批下载 |
| 每批完成后 | 立即可在界面下载该批 Excel（含「检测结果」+「汇总」） |
| 全部完成后 | 自动合并为全量 Excel（重新统计全量汇总），支持一键下载 |

**合并后的全量表结构**：所有批次的数据行按源表顺序拼接；「汇总」表重新统计全量的
any 不合规行数、all 不合规行数、纯白图 / 正常图 / 失败张数。

> 行号始终对应**源表行号**（批次 2 的「源表第 1001-2000 行」标签即为此意），方便回溯定位。

## 运行中取用已完成部分（v4.1）

长任务（大表 + 图片多）往往要跑很久。v4.1 让**不必等跑完**就能拿到已完成的部分：

| 方式 | 操作 | 适用场景 |
|------|------|----------|
| 分批下载 | 每批跑完，处理中卡片出现「下载本批」 | 数据 > 1000 行（自动分批） |
| 导出已完成部分 | 点「导出已完成部分」按钮 | 任何时候（含单批内、分批跑批过程中） |

「导出已完成部分」生成的 Excel：
- 结构与本插件正式输出**完全一致**（「检测结果」+「汇总」两个工作表）
- **已完成的行**：正常给出双维度判定、纯白数、明细
- **未完成的行**：判定列标为「未处理」，其余列原样保留
- 汇总表给出：数据总行数 / 已完成行数 / 未处理行数 + 已完成部分的 any、all 不合规行数

> 该功能依赖「运行中定期落盘」（每 300 个 URL 或每 60 秒写一次进度），
> 所以导出的进度是**最近一次落盘**的状态，可能比界面实时数字略有延迟（最多 60 秒）。

### 进度文件（*.progress.json）

- 每个输出 Excel 旁有一个同名 `.progress.json`，记录逐行逐图的判定明细
- 作用：① 支撑「导出已完成部分」；② `--resume` 断点续跑；③ 意外中断后进度不丢
- 任务正常跑完后可删除；若想保留续跑能力则留着

### 命令行参数（无界面 / 脚本调用）

```bash
WhiteImageExcel.exe 商品.xlsx                       # 默认输出两种维度（任意一张即不合规 + 全部空白才不合规）
WhiteImageExcel.exe 商品.xlsx --rule any             # 仅 B 维度
WhiteImageExcel.exe 商品.xlsx --rule all             # 仅 A 维度
WhiteImageExcel.exe 商品.xlsx --rule any,all         # 双维度（显式，与默认一致）
WhiteImageExcel.exe 商品.xlsx --chunk-size 1000      # 分批行数上限（默认 1000，超过即分批）
WhiteImageExcel.exe 商品.xlsx --chunk-size 0         # 强制不分批（一次性跑完）
WhiteImageExcel.exe 商品.xlsx --persist-every 300    # 每 N 个 URL 落盘一次进度（默认 300；0=只在结束时落盘）
WhiteImageExcel.exe 商品.xlsx --export-partial 商品_空白图结果.xlsx.progress.json -o 部分结果.xlsx
                                                     # 不跑检测：从进度文件导出「已完成部分」结果
WhiteImageExcel.exe 商品.xlsx -o 结果.xlsx           # 指定输出名
WhiteImageExcel.exe 商品.xlsx -l 50                  # 仅前 50 行（试跑）
WhiteImageExcel.exe 商品.xlsx -w 16                  # 16 线程并发（默认 8）
WhiteImageExcel.exe 商品.xlsx -s 查询结果             # 指定工作表（默认第一个）
WhiteImageExcel.exe 商品.xlsx -c 主图链接,详情图地址  # 指定 URL 列（逗号分隔）
WhiteImageExcel.exe 商品.xlsx -r                     # 断点续跑（跳过已完成行）
```

> `--rule` 默认 `any,all`，即**同时输出两种维度**到 Excel 不同列，**互不影响**。
> 旧版 `--rule any` 单值语法仍兼容（等价于 `--rule any`）。
>
> `--chunk-size` 默认 `1000`：数据超过该行数即分批。CLI 分批运行时会在控制台打印
> 每批的产出路径（`[批次完成] 第 i/N 批 → xxx_批次i.xlsx`），全部完成后打印全量结果路径。

## 输出 Excel 说明

- **检测结果** 表：原表所有列 + 每行追加
  - 每个图片列：`{列名}·判定`（是/否）、`{列名}·纯白数`、`{列名}·明细`（如 `纯白图、正常图`）
  - 行级双维度结果（按你勾选的维度顺序输出对应列）：
    - `一张即不合规`（B）—— 当勾选 any 时输出
    - `全部空白才不合规`（A）—— 当勾选 all 时输出
  - 行级汇总：`纯白图总数`、`图片总数`
- **汇总** 表：输入/输出文件、URL 列、**判定维度（同时列出）**、源表数据总行数、
  **本批区间**（分批时）、本批处理行数、已处理行数、
  **不合规行数（一张即不合规 · any）**、**不合规行数（全部空白才不合规 · all）**、
  纯白图/正常图/失败张数、耗时、生成时间。
- **全量合并表**（分批时）：「汇总」表额外记录**批次数**与**总耗时**。

### 双维度差异示例

| 行内容 | 图片总数 | 纯白数 | B · 一张即不合规 | A · 全部空白才不合规 |
|--------|----------|--------|------------------|----------------------|
| 主图+详情图 都白 | 2 | 2 | 是 | 是 |
| 主图白 / 详情图正常 | 2 | 1 | 是 | 否 |
| 主图+详情图 都正常 | 2 | 0 | 否 | 否 |

> 实际数据示例：`input.xlsx`（288 行）→ any 不合规 167 行，all 不合规 167 行
> （该数据集每行单图，所以两种维度相等；多图行 any ≥ all）

## 判定逻辑（纯像素，无模型）

- 近白像素占比（RGB 均 > 240）≥ 阈值（默认 0.99）→ **纯白图**
- 颜色种类极少（≤ 3）且整体浅色 / 单色 → **纯色空图**
- 其余 → **正常图**（含商品主体）

**不合规维度（v3 双维度同时计算）**：
- `any`（B）：该行 `纯白图张数 > 0` → 不合规
- `all`（A）：该行 `图片总数 > 0 且 纯白图张数 == 图片总数` → 不合规（即全部为空白）

## 输入要求

- 标准 `.xlsx`，首行为表头。
- 图片地址列**自定义**：界面勾选或命令行 `--cols` 指定；未指定时自动识别含「图片/图/url/链接/主图/详情图/image/img」的列。
- 单格支持多个 URL（逗号 / 换行 / 分号 / 空格分隔）。
- 图片 URL 需公网可访问（本机下载后分析）；若返回「失败」多为源站防盗链，需换可访问源图。

## 重新打包（开发用）

```bash
cd white-image-detect
python -m PyInstaller --onefile --noconsole --name WhiteImageExcel \
  --hidden-import numpy --hidden-import PIL --hidden-import openpyxl --hidden-import requests \
  white_excel_app.py
# 产物：dist/WhiteImageExcel.exe
```

源码入口：`white_excel_app.py`（HTML 向导 + 无界面双模式，内置 `PAGE_HTML` 模板与本地 http 服务），核心逻辑见 `batch_excel.py`（`run_batch` / `run_batch_chunked` / `merge_chunks`）与 `white_detect.py`。

> ⚠️ **打包时若报 `[safe-delete] 操作失败`**：PyInstaller 覆盖同名 exe 时用的 `os.remove`
> 会被安全删除钩子拦截（回收站操作失败）。解决：先把旧 exe 改名或删除，再执行打包：
> ```powershell
> Move-Item dist\WhiteImageExcel.exe dist\_old.exe -Force
> ```

## 排错 / 备注

- **首次启动稍慢**：单文件 exe 每次运行会把内置的 Python+依赖解压到临时目录（含 numpy 等二进制），杀毒软件扫描可能额外耗时 10~40s，属正常。
- **OpenBLAS 线程**：脚本已在启动时固定 `OMP_NUM_THREADS=1` 等，避免 NumPy 在冻结 exe 内派生大量自旋线程抢占 GIL 导致卡顿；**不要**在用户机器上强行调大该值。
- **调试日志**：exe 运行时同目录生成 `debug_trace.log`（启动时间、进度、异常），若某次检测异常可凭此定位；正常可删。
- **不消耗 token / 不上云**：判定完全本地像素分析，不联网推理；仅下载 Excel 里的图片 URL 到本机分析，不会把图片发往任何 AI 服务。
- **判定可调**：阈值 `white_threshold`（默认 0.99）、颜色上限 `max_color_variety`（默认 3）如需放宽/收紧，改 `white_detect.py` 后重新打包即可。
- **分批下载有效期**：批次结果保存在本次运行的临时目录，进程关闭后失效；需要留存请下载到本地。
  全量文件同理。CLI 模式下批次文件与全量文件都写在输出目录，是持久文件。
- **v4 → v4.1 兼容性**：输出 Excel 的结构、列名、API 全部不变；仅新增运行中定期落盘、
  `--persist-every`、`--export-partial` 及 GUI「导出已完成部分」按钮。
- **v3 → v4 兼容性**：API 与列名完全不变，仅新增 `--chunk-size` 参数与分批下载能力；
  数据量 ≤ 1000 行时行为与 v3 完全一致（单批、文件名不变）。
- **v2 → v3 兼容性**：`是否不合规` 列改名为 `一张即不合规`（B 列），新增 `全部空白才不合规`（A 列），
  `纯白图总数`/`图片总数` 列保持不变；旧脚本读 `summary.noncompliant_rows` 仍可（已映射为 any 维度）。