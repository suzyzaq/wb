# ── Python ──
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# ── PyInstaller 构建产物 ──
build/
*.spec

# ── dist 目录：只保留主 exe，其余（调试日志、旧版本）忽略 ──
dist/*
!dist/WhiteImageExcel.exe

# ── 运行时 / 调试产物 ──
*.log
*.progress.json
已完成部分.xlsx
待处理*.xlsx

# ── 编辑器 / 系统 ──
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini
